export default {
    install(Vue) {
        // const ws = new WebSocket("ws://localhost:8000/ws");
        // Vue.prototype.$ws = ws;
        // Vue.prototype.$onmessage = (func) => {
        //     ws.onmessage = (event) => {
        //         let data = JSON.parse(event.data);
        //         func(data)
        //     }
        // }
    }
}